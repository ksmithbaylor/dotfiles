servers = (
  {
    address = "chat.freenode.net";
    chatnet = "Freenode";
    port = "6667";
  }
);

settings = {
  core = {
    real_name = "Kevin Smith";
    user_name = "kjs-";
    nick = "kjs-";
  };
  "fe-text" = { actlist_sort = "refnum"; };
};

chatnets = { Freenode = { type = "IRC"; }; };

ignores = ( {
    level = "JOINS PARTS QUITS NICKS";
    network = "Freenode";
 } );
